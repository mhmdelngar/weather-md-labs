import 'package:flutter/material.dart';

class WeatherService extends StatelessWidget {
  const WeatherService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
